export type User = {
    avatar_url: string,
    login: string,
    bio: string | null,
    followers: string,
    following: string,
    public_repos: string,
    location: string | null,
    id: number
}